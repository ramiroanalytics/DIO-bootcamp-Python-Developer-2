lista = []

lista.append(1)
lista.append("Python")
lista.append([40, 30, 20])

print(lista)  # [1, "Python", [40, 30, 20]]

"""
lista.clear()
Remove todos os elementos da lista, deixando-a vazia.

lista = [1, 2, 3]
lista.clear()
print(lista)  # Saída: []


lista.copy()
Cria uma cópia superficial da lista. Útil para não modificar a lista original.

lista2 = lista.copy()


lista.count(x)
Retorna quantas vezes o elemento x aparece na lista.

lista = [1, 2, 2, 3]
print(lista.count(2))  # Saída: 2


lista.extend(iterável)
Adiciona todos os elementos de outro iterável (lista, tupla, etc.) ao final da lista.

lista = [1, 2]
lista.extend([3, 4])
print(lista)  # Saída: [1, 2, 3, 4]


lista.index(x)
Retorna o índice da primeira ocorrência do elemento x.

lista = [1, 2, 3, 2]
print(lista.index(2))  # Saída: 1


lista.pop([i])
Remove e retorna o elemento no índice i. Se i não for especificado, remove o último elemento.

lista = [1, 2, 3]
lista.pop()  # Remove 3
lista.pop(0) # Remove 1


lista.remove(x)
Remove a primeira ocorrência do elemento x.

lista = [1, 2, 3, 2]
lista.remove(2)
print(lista)  # Saída: [1, 3, 2]


lista.reverse()
Inverte a ordem dos elementos da lista.

lista = [1, 2, 3]
lista.reverse()
print(lista)  # Saída: [3, 2, 1]


lista.sort()
Ordena a lista em ordem crescente por padrão. Aceita parâmetros como reverse=True para ordem decrescente.

lista = [3, 1, 2]
lista.sort()
print(lista)  # Saída: [1, 2, 3]


len(lista)
Retorna o número de elementos na lista.

print(len(lista))  # Saída: número de elementos

"""

